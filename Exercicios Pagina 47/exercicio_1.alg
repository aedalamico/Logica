
inicio
    variavel inteiro num <- 100
    constante inteiro cont <-1
    enquanto (num < 200) faz
        num <- num + cont
        se ((num % 2) =/= 0) entao
            escrever num, "\n"
        fimse
    fimenquanto                    
fim
