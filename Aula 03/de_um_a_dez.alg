inicio
    variavel inteiro limite <- 10
    variavel inteiro contador <- 1	
    constante inteiro incremento <- 1
    
    enquanto (contador <= limite) faz
    
        escrever contador,"\n"
        contador <- contador + incremento
        
    fim enquanto
fim
